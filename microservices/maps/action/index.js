const action = async (app) => {
  // app.action('maps:{name}', async (meta, res) => {
  //   try {
  //     const client = await app.options.db;
  //     res.json({});
  //   } catch (err) {
  //     process.exit(err);
  //   }
  // });

  return app
}

export { action }
