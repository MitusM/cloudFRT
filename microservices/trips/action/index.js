const action = async (app) => {
  // app.action('article:{name}', async (meta, res) => {
  //   try {
  //     const client = await app.options.db;

  //     res.json({});
  //   } catch (err) {
  //     process.exit(err);
  //   }
  // });

  return app
}

export { action }
